[project]
name = "curriculum-gen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit",
    "python-dotenv",
    "pypdf2",
    "python-docx",
    "requests",
    "openai",
    "groq",
]
