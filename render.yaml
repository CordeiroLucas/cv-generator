services:
  - type: web
    name: curriculum-gen
    env: python
    buildCommand: pip install uv && uv sync --frozen
    startCommand: uv run streamlit run main.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.12
      - key: STREAMLIT_SERVER_HEADLESS
        value: "true"
      - key: STREAMLIT_BROWSER_GATHER_USAGE_STATS
        value: "false"
